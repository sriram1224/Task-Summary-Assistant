import React from "react";

const Logo = () => (
  <div className="flex items-center gap-2">
    <span className="text-3xl font-extrabold tracking-tight text-blue-600 font-serif">
      Snap Task
    </span>
    <span className="text-lg font-semibold text-gray-500"></span>
  </div>
);

export default Logo;
